— Loops: for-in, while, repeat-while, break/continue, labeled loops.

1️⃣ У Swift є три основні види циклів. Назви їх та коротко поясни, коли який застосовувати.

2️⃣ У чому різниця між while та repeat-while?

3️⃣ Що роблять оператори break та continue у циклах?

4️⃣ Як у Swift працюють labeled loops? Для чого вони потрібні?

5️⃣ Чим відрізняється ітерація по масиву for-in від ітерації через for i in 0..<array.count?

Відповіді:

1. Так, існують такі основні цикли - цикл For - In, цикл while, цикл Repeat - While. 
Цикл (for - in) використовуємо коли нам слід пройтись по масиву, діапазоні чисел чи символах у рядку. Де ми всерединці циклу призначаємо кожному елементу тимчасову константу.
Цикл while, використовуємо коли нам не відома кількість ітерацій та допопоки умова не стане хибною.
Цикл Repeat - while - навпаки, допоки умова є істиною - виконується блок коду.
2. У тому що, блок коду виконується при while(допоки умова не буде false), при repeat-while(допоки умова буде true)
3. Оператор break - виходить з циклу негайно. Оператор continue -  пропускає поточну ітерацію і переходить до наступної.
4. Labeled loops - це механізм який дозволяє додавати мітку до циклу щоб переривати цикл. Особливо коли є вкладені цикли - коли нам важливий вихід як із внутрішнього так і зовнішнього циклу.
5. Тим що у циклі for i in 0..<array.count -вказується певний проміжок (до коли) нам потрібно проходись по масиву. Тут використовується певна конструкція array.count. Яка підраховує кількість масивів.

Chat Answers.
1. Види циклів
✅ Ти все назвав правильно.
🔹 Можна додати: for-in не лише масив/діапазон — він також ітерується по словниках (отримує кортеж key-value).
🔹 Для while/repeat-while формулюй так:

while перевіряє умову перед виконанням;

repeat-while виконує тіло мінімум один раз, бо умова перевіряється після.

2. Різниця while / repeat-while
✅ Суть правильно передав.
🔹 На співбесіді коротко: «repeat-while — це do-while у Swift, тіло виконується щонайменше один раз».

3. break / continue
✅ Дуже чітко.
🔹 Можна додати: break з labeled loop дозволяє вийти з зовнішнього циклу, continue з labeled loop — перейти до наступної ітерації зовнішнього.

4. Labeled loops
✅ Правильно.
🔹 Можна сказати: «Лейбл — це ім’я циклу, після break або continue можна вказати це ім’я».
Приклад:

outerLoop: for i in 1...3 {
    for j in 1...3 {
        if j == 2 { break outerLoop }
    }
}


5. for-in vs for i in 0..<array.count
✅ Суть передав.
🔹 Можна уточнити: for element in array дає елементи масиву напряму.
А for i in 0..<array.count дає індекси, які можна використовувати для доступу array[i].
Тобто виодяться не числа, а їх індекси.
Властивість .count - викоритовується для підрахунку кількості елементів у колекціях.