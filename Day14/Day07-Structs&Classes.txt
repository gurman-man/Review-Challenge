— Structs vs Classes: init, properties, methods, mutating, inheritance.

1. 
У чому ключова різниця між struct та class у Swift?
(пам’ятай про value vs reference type)

2. 
Що таке memberwise initializer у struct і чому у class його немає за замовчуванням?

3. 
Що таке mutating method у struct і коли потрібно додавати ключове слово mutating?

4. 
Як у class працює наслідування? І чому struct не підтримує наслідування?

5. 
Що означає, що class має reference semantics, а struct — value semantics? Наведи приклад із копіюванням змінних.

Відповіді:
1. У тому що structs - це value types (тип - значення). Class -  reference type(тип - посилання). Ключова різниця між ними така що, structs при привсоєнні або передачі сторюю незалежну нову копію та ніяк не впливають на іншу, а classes - посилаються на той самий об'єкт та внесені зміни впливають на інші посилання. Ще - класи можуть наслідуватися, структури - ні. Метод deinit() підтримує лише клас для прибирання ресурсів. Mutaing keyword використовуєм лише у structs для зміни властивостей у методі.

2. memberwise initializer - це автоматичні ініціалізатори які приймають значення для властивостей.

3. Ключове слово mutating слід додавати до методів у структурах, щоб змінити їхні властивості. Muthating метод доозволяє змінювати вастивості.

4. Наслідування дозволяє розширити можливості батьківського класу. Ми просто дописуємо : (назва класу який хочемо передати) до батькіськго класу. Stuct не підтримує наслідування, бо це тип - значення. 

5. незнаю що ознчає. Але наведу пприклад із копіюванням змінних у класі. 
class UserClass {
	var name: String
	init(name: String) {
		self.name = name
	}
}

var a = UserClass(name: "Max")
var b = a
b.name = "Olha"
print(a.name) // виведе "Olha"


Сhat Answers:
1️⃣ Struct vs Class
✅ Ти правильно сказав: Struct = value type, Class = reference type.
Додай:

Struct копіюється при присвоєнні, тому кожна копія незалежна.
Class передає посилання на один і той же об’єкт, зміни видно скрізь.
Class підтримує deinit, Struct — ні.
Struct за замовчуванням має memberwise init, Class — ні.
Це вже виглядатиме як повноцінна відповідь.

2️⃣ Memberwise initializer
✅ Суть передав: це автоматично створений ініціалізатор у Struct, що приймає параметри для всіх властивостей.
💡 Додай: якщо у Struct ти створиш свій init, Swift перестане створювати memberwise init автоматично.
Class завжди пишеться вручну.

3️⃣ Mutating
✅ Правильно: без mutating метод Struct не може змінювати власні властивості.
💡 Також це ключове слово дозволяє в методі Struct «переприсвоювати self» (повністю замінювати структуру).

4️⃣ Наслідування
✅ Все вірно: Class може наслідувати інший Class, Struct — ні.
💡 Додай: Struct можна «розширювати» через protocol + extension (часто на співбесідах питають, як ти обійдеш відсутність наслідування).

5️⃣ Value vs Reference semantics
Тут якраз і треба пояснити:

Value semantics (Struct): при копіюванні створюється новий екземпляр, зміни не впливають на інші копії.

Reference semantics (Class): при копіюванні копіюється посилання на той самий об’єкт, зміни видно всюди.