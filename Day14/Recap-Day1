- Основи: variables, constants, type annotations, string interpolation.

1. Яка різниця між var і let у Swift?
2. Що таке type inference і як воно працює у Swift?
3. Коли і для чого варто явно вказувати тип змінної (type annotation)?
4. Як працює string interpolation у Swift і чим вона відрізняється від конкатенації рядків?
5. Чи можна змінювати значення властивості у структурі, якщо сама структура оголошена як let? Поясни.

Відповіді:

1. Var це змінна, let - константа. Var це сховище даних, значення якого можна змінити. Let - це сховище даних, яке встановлюється один раз і його не можна змінити.
2. type inference - це види типів. Бувають різні види: цілочисельний Int, тип з подвійною комою - Double, булевий тип - Bool, тип з десятковими значеннями - Float та інші.
3. Явно слід вказувати тип змінної, коли ми хочемо надати конкретики, тобто бачити тип змінної. Так легше зорієнтуватися, що варто писати.
4. string interpolation дає можливість передавати змінну у рядок. Я не знаю що означає слово конкатенація. Але загугливши бачу що це об'єднання двох або більше рядків в один новий рядок. За допомогою string interpolation теж можливо. Але різниця між ними в тим що в рядок можеш передавати різні змінні.
5. не зрозумів про яку ти структуру говориш. Про struct?

Chat Answers:

1. Var vs Let
✅ Добре. Можна ще додати, що let створює immutable binding (посилання незмінне), але якщо це reference type (клас), то властивості всередині можна змінювати. Це часто питають.

2. Type inference
Тут трошки неточно 🙂
Type inference — це не «види типів», а здатність компілятора автоматично визначати тип змінної з того, яке значення ти їй присвоюєш.

3. ✅ Суть вірна, можна сформулювати так: “Коли компілятор не може точно визначити тип або коли хочемо чітко задати тип для читабельності/безпеки.”

4. String interpolation
✅ Суть правильна. Можна дати приклад:

let name = "Max"
print("Hello \(name)") // інтерполяція
print("Hello " + name) // конкатенація

5. Тут дійсно часто плутаються:
– Якщо ти створив екземпляр структури через let, ти не можеш змінювати її властивості, бо сама структура — value type.
– Якщо оголосив через var, тоді можна змінювати властивості.

