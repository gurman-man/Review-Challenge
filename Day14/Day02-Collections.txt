День 2 – Collections 📖 Теорія: arrays, sets, tuples, dictionaries, enums (raw/associated values).

1️⃣ Arrays
У чому різниця між Array і Set у Swift?

2️⃣ Sets
Як Swift визначає, що два елементи у Set однакові? Що має реалізовувати елемент?

3️⃣ Tuples
Чим кортеж (tuple) відрізняється від масиву?

4️⃣ Dictionaries
Що буде, якщо у словнику Dictionary ти додаси значення за вже існуючим ключем?

5️⃣ Enums
Поясни різницю між raw values та associated values у enum у Swift.


Відповіді:

1. Array це колеції значень які зберігаються як єдине значення. Set - це невпорядковані колекції значень, які 1) не можуть мати одне й те саме значення, 2) зберігаються / розташовуються випадково. У той же час Array може містити однакові значення, але зберігатися послідовно.
2. Ну він проходиться по елементам одного типу і визначає чи вони збігаються чи ні.
3. Тим, що кортеж може зберігати значення різного типу. Він групує кілька значень в єдине складне речення.
4. Буде помилка у компіляторі. Потрібно буде обрати яке значення використовувати. Він не дозволить сторити значення за вже існуючим ключем.
5. Enum це спеціалбний тип даних, який дозволяє визначати набір пов'язаних між собою значень і працювати з  ними у більшструтуровані формі. Associate values - це  пов'язані значення, що означає що ми можемо додавати певну додаткову інформацію до нашо case. Raw values - це не оброблені значення, що означають що це просто case без додаткового опису що він має робити. 

Chat Answers: 
1. Array vs Set
✅ Ти правильно описав: Array — впорядкований, може містити дублікати; Set — невпорядкований, унікальні значення.
💡 Ще можна додати: доступ до елемента в Array — через індекс, у Set — пошук за значенням (хеш-таблиця під капотом).

2. Sets — як визначається унікальність
Твоя відповідь трохи розмита 🙂
💡 У Swift елементи Set повинні відповідати протоколу Hashable (тобто мати hash(into:) + ==), тоді Swift може визначити рівність і унікальність.

3. Tuple vs Array
✅ Правильно: Tuple може містити різні типи, а Array — лише один тип.
💡 Ще додай: Tuple — це фіксований набір значень, його довжина й типи визначаються при створенні (не можна додати ще одне значення пізніше).

4. Dictionaries — додавання за вже існуючим ключем
Тут неточність 🙂
💡 Swift не видасть помилку. Якщо ти додаси значення за існуючим ключем — старе значення просто заміниться новим:

var dict = ["a":1]
dict["a"] = 2 // тепер "a":2

5. Enum raw vs associated values
Ти описав загалом правильно 👍
💡 Додати: Raw values — це базовий тип (String, Int…), який автоматично присвоюється case'ам і однаковий для всіх екземплярів. Associated values — дозволяють при кожному створенні case передати різні дані:

enum Status {
    case success(code: Int) // associated
    case error(String)
}

enum Direction: String { // raw
    case north = "N"
    case south = "S"
}