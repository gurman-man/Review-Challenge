— Typecasting: as?, as!.

🧩 Typecasting — Питання для співбесіди

1.
Що таке typecasting у Swift і для чого він використовується?

2.
Яка різниця між as, as? і as!?

3.
Коли використовувати as?, а коли as!?

4.
Що буде, якщо примусове приведення типу (as!) не вдасться?

5.
Що повертає as?, якщо приведення типу не вдалось?

6.
Можна навести приклад з класами або масивом Any, де використовується приведення типу?

7.
У чому різниця між upcasting і downcasting?

8.
Для чого може бути потрібне upcasting (as) у коді?

9.
Як безпечно обробити об’єкт, тип якого невідомий, використовуючи typecasting?

10.
Чому typecasting часто використовують у зв’язці з protocols або UITableViewCell?


Відповіді:

1. 
typecasting - це спосіб перетворення об'єкта одного типу в інший. Int -> Double.

2. 
as - це оператор який використовується для звичайного перетворення 
as? - це оператор який використовується для опціонального перетворення (безпечного) тобто якщо перетворення неможливе поверне nil.
as! - це оператор який використовується для примусового перетворення. Використовується коли приведення типу точно буде успішним.

3. 
as? - коли ми хочемо безпечно перетворити тип.
as! - щоб швидко привести тип та коли ми точно впенені що приведення буде успішним.

4. 
буде краш.

5. 
поверне nil. А що до речі nil? - я знаю що це означає - нічого, тобто 0. Поясни ще ти.

6.
let object: Any = "Hello"
if let str = object as? String {
    print("This is a string: \(str)")
}else {
    print("Cannot convert object to string")
}

7. 
upcasting - це приведення значення типу з типу підкласу в тип суперкласу.
downcasting - це перетворення значення з типу скперкласу в тип підкласу.

8. 
нуу ми часто використовуємо upcasting щоб перетворити наприклад наш сторіборд з певним індентифікатором (напрриклад TaskDetaiVC) до супер класу в коді - TaskDetailViewController. Це потрібно для того щоб ми могли звернутися до нього та змінити чи додати деякі дії до цього сторіборда. 

9.
потрібно використовувати тип Any. та as? - як у 6 питанні

10.
тому що нам часто потрібно звертатися до клітинки та налаштовувати її. Тому при приводому до типу UITableViewCell (сторіборд).

💬 Фідбек по відповідях

✅ 1. Правильно (з невеликим уточненням):
Typecasting — це не просто перетворення примітивних типів (Int → Double), а перевірка або приведення типу об’єкта під час виконання (runtime).
Тобто — не арифметичне перетворення, а робота з класами, протоколами, Any.

✅ 2. Дуже добре:
Вірно описав as, as?, as!.
as — для upcasting або type conversion (наприклад, Int → Double).
as? — безпечне приведення (optional).
as! — примусове приведення (force).

✅ 3. Абсолютно правильно:
Коротко і по суті — саме так і мають відповідати на співбесіді.

✅ 4. Правильно:
Так — as! призведе до крашу, якщо приведення не вдасться.

⚠️ 5. Частково правильно (уточнення про nil):
as? повертає nil, якщо приведення неможливе.
🔹 nil у Swift означає відсутність значення (а не 0!).
Це не те саме, що нуль у числах.

6. Дуже добре (відмінний приклад):
Класичний, правильний приклад безпечного приведення з Any.

7. Ідеально:
Upcasting і Downcasting описані чітко та з правильною термінологією.

⚙️ 8. Хороша спроба, але трохи неточно:
Upcasting частіше використовують, коли хочуть працювати через базовий тип.
Наприклад:

let dog = Dog()
let animal: Animal = dog // upcasting

Тобто це не зовсім про storyboard, а радше про спадкування — перехід від підкласу до суперкласу.

✅ 9. Правильно:
Так — Any + as? — це класика для безпечної обробки невідомих типів.

✅ 10. Чудово:
Саме так. Часто в таблицях або колекціях нам треба привести клітинку або контролер до конкретного підкласу —
і тут застосовуємо as? або as!.