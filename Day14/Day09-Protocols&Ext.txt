— Protocols + Extensions: protocol inheritance, protocol extensions.

1.
Що таке protocol у Swift і для чого він використовується?

2.
Чи можуть протоколи мати властивості? Якщо так, то які (var/let, get/set)?

3.
Чим відрізняється class inheritance від protocol inheritance?

4.
Чи може клас/структура реалізовувати кілька протоколів одночасно?

5.
Наведи приклад, коли краще використати protocol, а не class inheritance.

6.
Що таке protocol inheritance і для чого воно потрібне?

7.
Що таке protocol extension? Чим воно відрізняється від звичайного extension?

8.
Чи можна у protocol extension додати дефолтну реалізацію методу?

9.
Чи можна у protocol extension додати stored property? Чому так/ні?

10.
Що таке protocol composition (&) і навіщо його використовують?

11.
У чому різниця між тим, коли ми викликаємо метод з protocol extension і коли з class conforming to protocol? (static vs dynamic dispatch)
12.
Наведи простий приклад, де ти б використав протокол у реальному додатку (наприклад, делегування, мережевий сервіс, збереження даних).

Відповіді:

1. Protocol - це список опису вимогог до реалізації, які повинен виконати клас, структура або  перерахування.
2. Тако  можуть мати властивості але не computed (обчислювальні). У протоколах властивість повинна мати get для зчитування значення або set для змінювання. Або ж і те разом.
3. Класи можуть успадковувати крім інших класів також структури, протоколи, перерахування. А протоколи можуть лише успадковуватись від інших протоколів.
4. Може.
5. Коли нам слід просто створюти певний опис вимог завдань під різні задачі.
6. Воно потрібне для того щоб інші протоколи підписувались на ці вимоги.
7. Protocol extenssion - це розширення до вже інуючого протоколу, яке дозволяє додавати обчислені властивості та методи. Звичайне extenssion додають до певного типу,  Protocol extenssion - до протоколу.
8. Що таке дефолтна реалізація методу?
9. Ні не можна, лише обчислювальну властивість. Тому що ми будемо звртатись до певного типу.
10. не знаю що таке protocol composition (&)
11. не знаю
12. ну в залежності який додаток


Chat Answers

1. ✅ Правильно. Можеш ще додати: протоколи не мають реалізації (окрім extension), а лише описують контракт.

2. ❌ Трохи неточно.
У протоколах усі властивості завжди описуються як var, навіть якщо тип реалізує їх як let.
Вони можуть бути computed або stored в реалізації.
Головне: протокол каже лише get чи get set.
➝ Приклад:
protocol Example {
    var name: String { get }
    var age: Int { get set }
}

3. ❌ Тут помилка.
Класи у Swift можуть успадковувати лише один клас (single inheritance).
Але вони можуть реалізовувати кілька протоколів одночасно.
Протоколи можуть успадковуватися від кількох протоколів.

4. ✅ Правильно — клас/структура може реалізовувати багато протоколів.

5. ✅ Вірно, але розгорну: протокол використовують, коли важлива поведінка (behavior), а не спадковість даних (state). Наприклад: Codable, Equatable, UITableViewDelegate.

6. ✅ Добре, тільки уточнюй: протокол може наслідувати інші протоколи, щоб розширити список вимог.

7.✅ Дуже добре. Можна ще додати: protocol extension дозволяє робити "дефолтну реалізацію" для всіх типів, які підписали цей протокол.

8. ❌ Ти запитав, що таке дефолтна реалізація 🙂
➝ Це коли у protocol extension ти додаєш метод з базовою поведінкою, і всі типи отримують його "за замовчуванням".

protocol Greeter {
    func greet()
}
extension Greeter {
    func greet() {
        print("Hello 👋")
    }
}

➝ Якщо тип не надасть власну реалізацію — використається ця.

9. ✅ Правильно — у protocol extension не можна додати stored property, лише computed. Бо extension не має власної пам’яті.

10. ❌ Ти ще не знаєш 🙂
➝ Protocol composition (&) = можливість вимагати кілька протоколів одночасно.

func printInfo(item: Codable & Equatable) {
    print(item)
}

Це як "і те, і те".

11. ❌ Важливий момент!
➝ Якщо метод реалізований у protocol extension, і ти викликаєш його через змінну типу protocol, буде використана дефолтна реалізація (static dispatch).
➝ Якщо тип реалізує свій метод — але виклик іде через протокол, то все одно викличеться дефолтний (крім @objc dynamic).

12. ✅ Твоя відповідь норм, але на співбесіді треба дати чіткий приклад.
Наприклад:
Делегати (UITableViewDelegate)
Збереження у UserDefaults через протокол Persistable
Мережевий сервіс APIServiceProtocol