— Access Control: static, lazy, access modifiers, self.

— Protocols + Extensions: protocol inheritance, protocol extensions.
— Optionals: if let, guard let, force unwrap, optional chaining, failable init.
— Error Handling: throws/rethrows, try?, try!, do-catch.
— Typecasting: as?, as!.
— Generics + FP: generic func/struct/protocol, map, filter, reduce.
— SOLID: S, O, L, I, D principles.

1.
Які рівні доступу (access control) існують у Swift і коли використовувати private vs fileprivate?

2.
Що таке static властивості та методи? У чому різниця між static і class (у класах)?

3.
Поясни, коли ми застосовуємо lazy var. Які обмеження є у lazy?

4.
Для чого використовується ключове слово self? У яких випадках Swift змушує писати self явно?

5.
У чому різниця між lazy, static і звичайною збереженою властивістю (stored property) з точки зору ініціалізації?


Відповіді:
(модуль - збірка коду, яку можна імпортувати - Свій додаток, UIKIT, SWIFTUI)
1. У Swift інуюють такі обмеження доступу:
public - дозволяє доступ з будьякого файлу та модуля 
private -обмежує доступ до елемента в межах класу / структури
internal - доступ елемента в межах модуля
file-private - доступ лише в межах поточного файлу.

2. static властивості це такі властивості які можна викликати без необхідності створювати екземпляр (об'єктт класу / структури.)

3. lazy var застосуємо тоді коли ми хочемо щоб властивість не одразу передалась.  Ця властивість виконується коли ми примусово звертаємось до неї щоб викликати. Або ж примусово коли вона використовується іншим об'єктом. Також lazy властивість не вимагає ініціалізації під час створення об'єкта. Обмеження є таке що lazy працює тільки з var. Let мусить мати значення одразу.

4. Self викоритовується для розпізнавання поточного екземпляра класу. Це часто використовується в кастомних ініціалізаторах. 

5. Різниця:
Lazy propertyможе бути не ініціалізована, до початку використання.
Stored property - повинна бути ініціалізована(явно) або і кастомному ініціалізаторі.
Static property - надає можливість не сторювати езкемпляр класу / структури для виклику цієї властивості. Також в середині типу може бути явно ініціалізована або ж взагалі не ініціалзована але якщо оголошена через static var. Static let - завжди має бути ініціалізована.

🔎Chat Answers

1. Access Control
✅ Загалом вірно. Є 5 рівнів:

open – доступ навіть для наслідування і override за межами модуля.
public – доступний з інших модулів, але не можна override.
internal – доступний тільки в межах модуля (за замовчуванням).
fileprivate – доступ тільки всередині того ж файлу.
private – доступ тільки в межах поточого типу та його extensions у тому ж файлі.

🔹 Ти не згадав про open, а це дуже часто питають на інтерв’ю.

2. Static vs Class
✅ Правильно сказав, що static можна викликати без створення екземпляра.
Але важливе уточнення:

static методи/властивості не можна override.
class (у класах) можна override в підкласах.

👉 Приклад:
class Animal {
    static func sound() { print("some sound") }
    class func move() { print("moving") }
}

class Dog: Animal {
    // override static func sound() { } ❌ не можна
    override class func move() { print("dog running") } ✅ можна
}


3. Lazy var
✅ Точно, lazy ініціалізується лише при першому доступі.
Додатково:

Використовують, коли властивість потребує "дорогих" обчислень або залежить від інших властивостей, які ще не ініціалізовані в момент створення об’єкта.

Не можна використати з let.

4. Self
✅ Правильно: self — це поточний екземпляр.
Swift змушує писати self, коли:

У тілі функції є name shadowing (наприклад, параметр методу зветься так само, як властивість).
Усередині escaping closure ([weak self] in → тоді self?.property).

5. Lazy vs Static vs Stored
✅ Влучно пояснив.
Додам для чіткості:

stored property – має бути ініціалізована під час створення об’єкта.
lazy – відкладає ініціалізацію до першого використання.
static – належить типу, а не екземпляру. Всі екземпляри класу бачать одне й те саме значення.