— Error Handling: throws/rethrows, try?, try!, do-catch.
— Typecasting: as?, as!.

🧩 Основні 10 питань по Error Handling (Junior рівень)

1.🔹 Що таке error handling у Swift і навіщо воно потрібне?

2.🔹 Як оголосити функцію, що може кидати помилку?

3.🔹 Для чого використовується ключове слово throws?

4.🔹 Для чого використовується ключове слово rethrows і в чому його відмінність від throws?

5.🔹 Як працює do-catch і як виглядає стандартна структура обробки помилок?

6.🔹 У чому різниця між try, try? і try!?

7.🔹 Що повертає try?, якщо функція кидає помилку?

8.🔹 Що станеться, якщо використати try!, а функція кине помилку?

9.🔹 Як створити власний тип помилки у Swift?

10. 🔹 У чому різниця між обробкою помилок через do-catch і через try??


Відповіді:

1. Error handling - це спосіб обробки помилки. Воно нам потрібне коли - замість результату, нам потрібно викинути помилку.
2. Наприклад:
enum NetworkError: Error {
    // види помилок
    case badURL
    case noInternet
    case noData
}
func request() throws -> String {
    if false {
        throw NetworkError.noInternet
    }
    return "Success"
}

3. Throws це ключове слово яке вказує що функція можe throw (кинути) помилку.
4. не знаю
5. У блоці do -  ми намагаємось виконати код, який може кинути помилку.
у блоці catch - ми ловим і обробляєм помилку.
Ось приклад: 

do {
    let result = try request()
    print(result)
} catch {
    throw NetworkError.noInternet
}

6. try - буде намагатись обробити значення, якщо його не буде кинеться помилка.
try? - теж саме що і у try
try! - буде намагатись обробити значення негайно, і якщо його там не має не кинеться помилка а буде краш.

7. try? - поврне значення за замовчуванням
8. буде краш
9. використати перерахування з різними випадками помилок - enum NetworkError: Error {
    // види помилок
    case badURL
    case noInternet
    case noData
}

10. у тому що через try? ми надаємо дефолтного значення помилці якщо буде nil. А у do catch ми може просто кинути помилку. 

CHAT ANSWERS:
1. ✅ Правильно
Суть передана вірно. Можна лише додати, що це робиться для безпечного виконання програми.

2. ✅ Правильно	
Синтаксис і логіка — правильні. Гарний приклад.

3. ✅ Правильно	Повністю вірно. Можна додати, що throws ставиться після сигнатури функції.

4. ⚠️ Потрібно знати!	
Це про rethrows. Використовується в функціях, які приймають функцію, що може кидати помилку.

5. ✅ Майже ідеально	Добре, але у catch не потрібно кидати помилку знову (throw), а обробити її (print, показати alert тощо).

6.⚠️ Частково правильно	
Ти написав, що try? “те саме що і try” — це не так. try? повертає опціонал, а try вимагає do-catch.

7. ❌ Неправильно	
Воно повертає nil, якщо сталася помилка. Не дефолтне значення.

8. ✅ Правильно	
Коротко і точно 😄

9. ✅ Правильно	
Це правильний спосіб створення власних помилок.

10.⚠️ Не зовсім	
try? не дає дефолтного значення — воно просто повертає nil, якщо є помилка. Але вірно, що do-catch дозволяє повноцінно обробляти помилку.